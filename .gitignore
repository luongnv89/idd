# OS files
.DS_Store
Thumbs.db

# Editor files
*.swp
*.swo
*~
.vscode/
.idea/
*.sublime-workspace
*.sublime-project

# Environment
.env
.env.local
.env.*.local

# Node (if skills are installed via npx)
node_modules/
package-lock.json
yarn.lock

# Python (if test scripts use Python)
__pycache__/
*.pyc
*.pyo
.venv/
venv/
env/

# Test artifacts
tests/tmp/
tests/output/
*.log

# Claude Code local state
.claude/

# Build outputs not committed (per refactor-plan-v10 §1, §6.0)
# - dist/skills/ and dist/agents/ are built by ./scripts/build.sh, not committed
# - dist/plugin/ is built in CI and shipped as a release artifact, not committed
# - dist/test-plugin/ is a throwaway scaffold for §6.0 experiments
dist/skills/
dist/agents/
dist/plugin/
dist/test-plugin/
